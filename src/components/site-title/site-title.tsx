import { FunctionComponent } from 'react';
import styles from './styles.module.scss';

export const SiteTitle: FunctionComponent = () => {
  return <h1 className={styles.container}>未来を生きる主役は国を変える。</h1>;
};
